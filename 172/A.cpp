#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main() {    
    /* Input */
    int a;
    cin >> a;
    /* Output */
    cout << floor(a + a*a + a*a*a) << endl;
    return 0;
}