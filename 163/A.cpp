#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;

int main() {
    /* Input */
    int R;
    cin >> R;

    cout << 2 * R * M_PI << endl;
    return 0;
}